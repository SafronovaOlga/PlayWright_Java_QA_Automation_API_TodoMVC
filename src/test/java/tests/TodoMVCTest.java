package tests;import org.junit.jupiter.api.Test;import static org.junit.jupiter.api.Assertions.assertTrue;public class TodoMVCTest extends BaseTest {   /* @Test    public void shouldAddNewTodoItem() {        page.navigate("https://demo.playwright.dev/todomvc/");                //"https://demo.playwright.dev/todomvc/#/");               // page.click("text=Vanilla JS");  .new-todo        page.waitForSelector(".new-todo").click();                page.fill(".new-todo", "Buy milk");        page.keyboard().press("Enter");                assertTrue(page.isVisible("text=Buy milk"));    }*/        /*@Test    public void shouldMarkItemAsCompleted() {        page.navigate("https://demo.playwright.dev/todomvc/");                page.click("text=Vanilla JS");        page.waitForSelector(".new-todo");                page.fill(".new-todo", "Buy milk");        page.keyboard().press("Enter");                page.click(".toggle");        assertTrue(page.locator(".todo-list li")                .first()                .getAttribute("class")                .contains("completed"));    }*/}